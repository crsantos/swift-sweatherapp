Simple Swift implementation of a Weather app importing a Framework
=========

Uses OpenWeatherMap as main API, wrapper inside a simple SDK, imported as submodule

Installation
--------------

```sh
git clone [git-repo-url] --recursive -branch develop
```